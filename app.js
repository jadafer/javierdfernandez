const format=document.createElement('link');format.rel='stylesheet';format.href='style-original-format.css';document.head.appendChild(format);const menu=document.querySelector('.menu'),links=document.querySelector('.links');if(menu&&links){menu.addEventListener('click',()=>{const open=links.classList.toggle('open');menu.setAttribute('aria-expanded',String(open))})}document.querySelectorAll('[data-year]').forEach(e=>e.textContent=new Date().getFullYear());
